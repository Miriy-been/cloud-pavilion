---
alwaysApply: true
scene: git_message
rules:
  - 提交信息全部使用简体中文，禁止英文、中英文混杂
  - 遵循 <类型>: <简短描述> 格式，类型限定：feat(新增)、fix(修复)、refactor(重构)、docs(文档)、style(格式)、perf(性能)、test(测试)、chore(杂项)
  - 描述高度精炼，控制在 15‑30 汉字，一句话讲清改动，不要多余解释、不要换行、不要句号
  - 只写改动行为，不写原因、不写背景，动词开头，例如：修复xx接口报错、新增文件筛选功能、重构存储模块逻辑
  - 禁止生成长段落、禁止列举一堆文件名称，只体现业务/模块改动
---

在此处编写规则，自定义 AI 生成提交信息的风格。
