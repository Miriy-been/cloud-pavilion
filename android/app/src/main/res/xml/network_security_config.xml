<?xml version="1.0" encoding="utf-8"?>
<!-- App 支持用户配置任意 Cloudreve 站点（可能为 HTTP 明文站点）。
     图片/API 走 dart:io 本就不受系统明文策略限制，若这里不放行明文，
     仅原生网络栈（just_audio/ExoPlayer 音频视频）会在 HTTP 站点下被拦截 -->
<network-security-config>
    <base-config cleartextTrafficPermitted="true"/>
</network-security-config>
